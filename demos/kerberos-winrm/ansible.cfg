[defaults]
inventory = inventories/hosts.yml
# Readable multiline task output (ansible-core 2.13+). Used when
# kerberos_diag_display_mode: stdout cats the diagnostic report.
stdout_callback = ansible.builtin.default
result_format = yaml
