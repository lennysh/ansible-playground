---
# Copy to hosts.yml (or point -i at your inventory) for sos_report / rh_case_update.
# SOS Report needs real Linux hosts with SSH (and become for sosreport).

all:
  hosts:
    localhost:
      ansible_connection: local
  # children:
  #   linux:
  #     hosts:
  #       host1.example.com:
  #         ansible_host: 192.0.2.10
