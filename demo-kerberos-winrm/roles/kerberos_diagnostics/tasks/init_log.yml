---
- name: Initialize Kerberos diagnostic log
  ansible.builtin.copy:
    content: |
      === EE KERBEROS DIAGNOSTICS START ({{ ansible_date_time.iso8601 | default(lookup('pipe', 'date -u +%Y-%m-%dT%H:%M:%SZ')) }}) ===

    dest: "{{ kerberos_diag_log_path }}"
    mode: "0644"
