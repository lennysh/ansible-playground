---
# Validate reconstructed PEM private key structure with openssl or ssh-keygen.
aap_survey_pem_key_validate: true

# Extra-var name for the squashed Password survey value (AAP survey question name).
aap_survey_pem_key_var: survey_pem_key
